import React from 'react'
import SearchContext from '../contexts/SearchContext'
import { useContext } from 'react'

export default function search() {
    const {studentSearch} = useContext(SearchContext)
  return (
    <div>
      
    </div>
  )
}
